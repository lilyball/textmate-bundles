<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple Computer//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>beforeRunningCommand</key>
	<string>saveModifiedFiles</string>
	<key>command</key>
	<string>revs=`"${TM_SVN:=svn}" log -q "$TM_FILEPATH"`

revs=`echo "$revs" | sed '/^------------------------------------------------------------------------$/d' | sed '/^$/d'`

revs=`osascript&lt;&lt;END
	set AppleScript's text item delimiters to {"\n","\r"}
	tell app "SystemUIServer" to activate
	tell app "SystemUIServer" to choose from list (every text item of "$revs") with prompt "Please choose an older revision:"
END`

revs=`echo "$revs" | tr '\r' '\n' | cut -d "|" -f 1`
revs=( $revs )

TMPFILE=`mktemp /tmp/tm-svn.XXXXXX` || exit 1

mv "$TMPFILE" "$TMPFILE.diff"
TMPFILE="$TMPFILE.diff"

"${TM_SVN:=svn}" diff -r${revs[0]#r} "$TM_FILEPATH" &amp;&gt; "$TMPFILE"

open -a TextMate "$TMPFILE" &amp;

#rm "$TMPFILE" # ideally, we'd do this, but it creates a
               # race condition, because 'open' is asynchronous.
</string>
	<key>input</key>
	<string>none</string>
	<key>keyEquivalent</key>
	<string>^A</string>
	<key>name</key>
	<string>Diff With Revision…</string>
	<key>output</key>
	<string>showAsTooltip</string>
	<key>uuid</key>
	<string>32E15B26-B444-11D9-8D63-000A95A89C98</string>
</dict>
</plist>
