{
    documentExtensions = ( html, htm, rhtml, shtml, phtml, php, php3, php4, php5, inc, cfm, cfml, dbm, dbml );
    draggedFileExtensions = ( png, PNG, jpeg, JPEG, jpg, JPG, gif, GIF );
    name = "Insert <img> tag"; 
    output = insertAsSnippet; 
    command = "img=$TM_DROPPED_FILE
alt=`echo $img | perl -pe 's/^(.*\\/)?(.*?)(\\..*)?$/$2/g' | tr '_-' ' '`
sips -g pixelWidth -g pixelHeight $img | awk '/pixelWidth/ { w=$2 } /pixelHeight/ { h=$2 } END { printf(\"<img src=\\\"'\"$img\"'\\\" width=\\\"%d\\\" height=\\\"%d\\\" alt=\\\"${0:'\"$alt\"'}\\\" />\", w, h) }'";
}
