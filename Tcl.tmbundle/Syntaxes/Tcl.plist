{
    /* The name as it will appear in the Syntax Highlight List */
    name = "Tcl"; 
    
    /* Which filetypes this highlighting should be applied to automatically */
    fileTypes = ( tcl ); 
    
    foregroundColor = "#ffffff"; 
    backgroundColor = "#000000"; 
    
    foldingStartMarker = "\\{\\s*\\\\?$}"; 
    foldingStopMarker = "^\\s*\\}"; 
    
    increaseIndentPattern = "\\{[^\"']*$"; 

    highlightPairs = ( ("[", "]"), ("{", "}") ); 
    smartTypingPairs = ( ("[", "]"), ("{", "}"), ("\"", "\"") ); 

    /* This is where the actual highlighting is defined. */
    patterns = (
		{
			name = "comments.tcl";
			"foregroundColor[2]" = "#9933CC";
			match = "(^|;)\\s*(#.*)$";
		},
        {   
            name = "keywords.tcl";
            "foregroundColor[1]" = "#CC7833";
            match = "(?:^|[[{;])\\s*(eval|expr|if|while|for|file|string|catch|source|info|set|unset|pid|global|array|puts|gets|read|open|close|socket|eof|fconfigure|parray|return|break|continue|switch|package|proc|exit|glob|regexp|regsub|foreach|llength|lindex|concat|list|split|join|incr|error)\\>";
        },
		{
			name = "keywords.if.tcl";
			"foregroundColor[1]" = "#CC7833";
			match = "(?:^|})\\s*(then|elseif|else)\\>";
		},
		{
			name = "dummy.escapes.tcl";
			match = "\\\\.";
		},
		{
			name = "keywords.variables.tcl";
			"foregroundColor[1]" = "#3387CC";
			"foregroundColor[2]" = "#3387CC";
			match = "(\\$)(\\w+)";
		}/*,
        {   name = "strings.double-quoted.tcl";
            foregroundColor = "#78CC33";
            begin = '"';
            end = '"';
            swallow = "\\\\.";
            patterns = (
                {   name = "strings.double-quoted.escapes.tcl";
                    match = "\\([abfnrtv]|0\d{2}|x[a-fA-F]{2})";
					foregroundColor = "#aaaaaa";
                }
            );
        }*/
    );
	
	uuid = "F01F22AC-7CBB-11D9-9B10-000A95E13C98";
}
