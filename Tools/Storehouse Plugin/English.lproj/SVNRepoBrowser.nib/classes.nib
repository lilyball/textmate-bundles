{
    IBClasses = (
        {CLASS = CXBrowserTableView; LANGUAGE = ObjC; SUPERCLASS = NSOutlineView; }, 
        {
            ACTIONS = {
                contextMakeDir = id; 
                contextRefresh = id; 
                getRootURLFromField = id; 
                sheetCancel = id; 
                sheetOK = id; 
                takeRootURLFrom = id; 
            }; 
            CLASS = CXSVNRepoBrowser; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                fCommitAnswerField = NSTextField; 
                fCommitPromptField = NSTextField; 
                fCommitPromptWindow = NSPanel; 
                fCommitURLDestination = NSTextField; 
                fCommitURLSource = NSTextField; 
                fCommitVerbField = NSTextField; 
                fGoButton = NSButton; 
                fOutlineView = NSOutlineView; 
                fSpinner = NSProgressIndicator; 
                fURLField = NSTextField; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            CLASS = CXSVNRepoNode; 
            LANGUAGE = ObjC; 
            OUTLETS = {delegate = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {"" = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = NSObject; LANGUAGE = ObjC; }
    ); 
    IBVersion = 1; 
}